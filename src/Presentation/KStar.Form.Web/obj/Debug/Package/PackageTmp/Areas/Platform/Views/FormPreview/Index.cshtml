@{
    /**/

    ViewBag.Title = "通用表单";
    Layout = "~/Views/Shared/_FormPreviewLayout.cshtml";
}
@section HeadStyle{
    @Html.Raw(ViewBag.Header)

}
@Html.Raw(ViewBag.FormInfo)
@section KStarFormJs{
    <script src="~/Scripts/wangEditor/3.1.1/wangEditor.min.js"></script>
    <script src="~/Scripts/wangEditor/xss.min.js"></script>
    <script src="~/Scripts/jquery-3.4.1.min.js"></script>
    @Html.Raw(ViewBag.Foot)
    <script type="text/javascript">
        //扩展
        KStarForm.formModelExtend = {
            data: function () {
                return {
                    formContentRules: {
                    }
                }
            },
            mounted: function () {
                this.$nextTick(function () {
                    setTimeout(function () {
                        $(".el-form-item__label").each(function (index, item) {
                            if ($(item).text() == "") {
                                //$(item).removeClass("el-form-item__label");
                                $(item).addClass("empty__label");
                            }
                        });
                    }, 1000);
                });
            },
            watch: {

            },
            //方法
            methods: {
                @Html.Raw(ViewBag.Methods)
                //start_end_date1(value) {
                //    FormContentModel.DATA.Start_Date = value;
                //},
                //start_end_date2(value) {
                //    FormContentModel.DATA.End_Date = value;
                //}
            },
            //计算属性
            computed: {
                isEdit: function () {
                    return this.vmFormType == 'Application' || this.vmFormType == 'Draft' || this.vmFormType == 'ReApproval' ? true : false;
                }
            },
        };
    </script>
    <script type="text/javascript">
        @Html.Raw(ViewBag.CustomScript)
    </script>
}
